import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  sectionContainer: {
    flex: 1,
  },
  titleText: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#333',
  },
});